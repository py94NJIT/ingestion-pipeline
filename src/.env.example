# ==============================================================================
# POSTGRESQL DATABASE CONFIGURATION TEMPLATE
# ==============================================================================
# Copy this file to a new file named ".env" and fill in your local credentials.
# The actual ".env" file is ignored by git and will not be pushed to GitHub.

DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432